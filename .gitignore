.cache/
__pycache__/
*.pyc
.venv/
venv/
data/run_report.json
data/quantum_tracker.xlsx
# NB: data/backends.json IS tracked — the GitHub Pages viewer fetches it, so the
# built snapshot needs to be in the repo. Re-commit it after a fresh run.

.DS_Store
