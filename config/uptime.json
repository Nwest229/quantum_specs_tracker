{
  "_note": [
    "Availability tracker config. 'devices' are qBraid device ids (the 'qbraid_id'",
    "field, e.g. 'iqm_garnet') exactly as they appear on qBraid. QBRAID_API_KEY is",
    "read from the environment, never stored here. Poll runs via cron (see README).",
    "'up_statuses' = statuses that count as available; everything else is downtime."
  ],
  "api_base": "https://api-v2.qbraid.com/api/v1",
  "devices_endpoint": "/devices",
  "auth_header": "X-API-KEY",
  "poll_interval_seconds": 300,
  "_devices_note": "Use [\"*\"] to track every device from the single API response, or list specific qrn ids like \"aws:iqm:qpu:garnet\". Simulators are skipped when exclude_simulators is true.",
  "devices": ["*"],
  "exclude_simulators": true,
  "up_statuses": ["ONLINE"],
  "log_path": "data/uptime_log.csv",
  "report_path": "data/uptime.json",
  "schedule_path": "config/uptime_schedule.json",

  "_crosscheck_note": [
    "Native cross-check: poll AWS Braket + IBM directly and compare their status",
    "against qBraid's (which normalises + can lag). Runs on its own schedule (SDKs",
    "+ AWS/IBM creds needed), writes crosscheck.json/_log — never touches uptime.json.",
    "ibm_instance is the (non-secret) CRN; tokens/keys come from the env only."
  ],
  "crosscheck_path": "data/crosscheck.json",
  "crosscheck_log_path": "data/crosscheck_log.csv",
  "crosscheck": {
    "aws_braket": true,
    "ibm": true,
    "ibm_instance": "crn:v1:bluemix:public:quantum-computing:us-east:a/0649c72b2be9486ba61b40d3e500bb5d:a8d5c529-b291-4322-a85b-2369c6298672::"
  }
}
